# 山裡海｜水上療癒

`https://clean.tw` 的網站成品。

這是**產生出來的檔案**，不要直接改這裡。
內容與樣式的原始檔在私人 repo `onelife-mini/shanlihai`，
改完跑 `python3 build-static.py`，再把 `site/` 的內容推到這裡。

- `index.html` — 整頁（CSS 與 JS 都內嵌，無外部相依）
- `img/` — 兩張實景照片與分享預覽圖
- `robots.txt` / `sitemap.xml`

由 Vercel 部署。
